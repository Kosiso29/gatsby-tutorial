import React from 'react';

const Error = () => {
    return (
        <div>
            Error
        </div>
    );
};

export default Error;

// 404.js is automatically called for pages that are not found